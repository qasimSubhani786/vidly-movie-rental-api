const API = "/api/";
exports.endPoints = {
  home: "/",
  genres: `${API}genres`,
  customers: `${API}customers`,
  movies: `${API}movies`,
  rentals: `${API}rentals`,
  users: `${API}users`,
  auth: `${API}auth`,
};
